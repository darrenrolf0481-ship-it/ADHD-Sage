## 2026-08-01 — projscan fix applied

**What:** Ran `projscan doctor` followed by `projscan fix` which automatically installed and configured ESLint (`.eslintrc.json`), Prettier (`.prettierrc`), and EditorConfig (`.editorconfig`). Updated `package.json` scripts/dependencies and updated the `graphify` knowledge graph via `graphify update .`.
**If things break, check:** `.eslintrc.json`, `.prettierrc`, `.editorconfig`, `package.json`, and `package-lock.json`.
